# Полный набор: ядро плюс модели, которые считают прямо на этой машине.
#
# Используется на ноутбуке, в Docker и в CI. В облаке ставится только
# requirements-core.txt — причина указана там же.
-r requirements-core.txt

# YOLOv8 ищет посторонние навалы на кадре с ESP32-CAM.
ultralytics>=8.3,<9.0
# CLIP zero-shot решает, годен ли хлеб. torch приходит вместе с ultralytics.
transformers>=4.40,<5.0
